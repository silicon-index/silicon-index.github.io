// Single place to point the static frontend at your deployed Worker.
// Update API_BASE after running `npm run deploy` in worker/ (see worker/README.md).
export const API_BASE = "https://silicon-index-admin-api.YOUR_SUBDOMAIN.workers.dev";
